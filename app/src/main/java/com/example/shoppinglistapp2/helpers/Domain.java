package com.example.shoppinglistapp2.helpers;

/**
 * Enum of supported domains for recipe website conversion
 */
public enum Domain {
    RECIPE_TIN_EATS,
    HELLO_FRESH,
    TASTE_COM_AU,
    FROM_SCRATCH,
    NOT_SUPPORTED
}
